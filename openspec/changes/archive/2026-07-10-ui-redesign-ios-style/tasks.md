# Tasks: UI Redesign

- [x] 1. Update `app_theme.dart` to enhance `GlassmorphismCard` (increase blur, add gradient borders, iOS-style curves).
- [x] 2. Update `history_screen.dart` to fix the `\${DateTime...}` string interpolation bug using the `intl` package (e.g., `DateFormat.Hm()`).
- [x] 3. Update `history_screen.dart` to show a better Avatar (e.g., first letter of app name) and improved typography.
- [x] 4. Update `settings_screen.dart` to use inset grouping (iOS style settings) instead of plain list tiles.
- [x] 5. Run `flutter analyze` and `flutter test`.